import React from 'react';
import styles from './Timer.module.scss'

const TimerHead = () => {
    return (
        <div className={styles.timer__head}>
            <span>Timer</span>
        </div>
    );
};

export default TimerHead;